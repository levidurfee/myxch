import { h, Component } from "preact";

export default class Addresses extends Component {
    render() {
        return (
            <div>
                Addresses
            </div>
        )
    }
}
